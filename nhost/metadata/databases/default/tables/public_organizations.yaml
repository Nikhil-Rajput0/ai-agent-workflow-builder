table:
  name: organizations
  schema: public
array_relationships:
  - name: external_events
    using:
      foreign_key_constraint_on:
        column: org_id
        table:
          name: external_events
          schema: public
  - name: org_members
    using:
      foreign_key_constraint_on:
        column: org_id
        table:
          name: org_members
          schema: public
  - name: step_runs
    using:
      foreign_key_constraint_on:
        column: org_id
        table:
          name: step_runs
          schema: public
  - name: workflow_runs
    using:
      foreign_key_constraint_on:
        column: org_id
        table:
          name: workflow_runs
          schema: public
  - name: workflows
    using:
      foreign_key_constraint_on:
        column: org_id
        table:
          name: workflows
          schema: public
